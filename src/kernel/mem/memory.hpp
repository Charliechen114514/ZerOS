#pragma once
#include <cstddef>
#include <expected>
#include <span>

namespace ZerOS {

struct MemoryAllocator {
    enum class MemoryAllocationErr { MemoryBufExceed, MemoryPoisoned };

    
};

} // namespace ZerOS
